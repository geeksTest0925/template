import { ref } from "vue";
export const USER_BUTTONS = ref(null);
export const USER_ALL_MENU_ID = ref(null);
export const USER_MENUS = ref(null);
export const CURRENT_MENU = ref(null);
export const USER_ROLE_ROUTE_NAME = ref(null);
export const TOKEN = ref('');
export const USER_INFO = ref(null);